# merge

合并对象（Object.assign）

```javascript
import merge from 'merge'

merge(clone: boolean, ...items: Object[])

merge(...items: Object[])

merge.recursive(clone: boolean, ...items: Object[])

merge.recursive(...items: Object[])
```

前端部分可以使用 Object.assign 合并对象
