# jest-watch-typeahead

Filter your tests by file name or test name   &#x20;

jest 测试结果后，使用这个插件进行过滤操作

```text
{
  "jest": {
    "watchPlugins": [
      "jest-watch-typeahead/filename",
      "jest-watch-typeahead/testname"
    ]
  }
} 
```

​
