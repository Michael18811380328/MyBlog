## 16-logging

* **功能**：提供灵活的日志记录系统，可设置日志级别、输出格式、输出目标（文件、控制台等）。

* **PyPI**：（内置）

* **GitHub**：（无独立 GitHub 仓库，属于 Python 标准库）

* **推荐使用**：强烈推荐，Python 日志记录必备。

```text
import logging
logging.basicConfig(level=logging.INFO)
logging.info('This is an info message.')
```

​
